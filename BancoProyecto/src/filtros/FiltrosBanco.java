package filtros;

public class FiltrosBanco {
	
	final static int CONCEPTO_MAX = 100;
	final static int CONCEPTO_MIN = 10;
	
	/*public static void concepto(String texto, double importe) {
		if(importe <= 0) {
			throw new SaldoInsuficiente("Es una cantidad negativa");
			}
		if(!Filtros.cumpleLongitud(texto, CONCEPTO_MAX, CONCEPTO_MIN)) {
			throw new ErrorFiltro("Error tamaño concepto");
		}
	}
	*/

}
